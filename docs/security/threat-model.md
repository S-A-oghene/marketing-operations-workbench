# Threat Model

Controls cover cross-workspace access, prompt secret exfiltration, restricted-data dispatch, provider/capability spoofing, duplicate side effects, webhook replay, malformed model output, covert browser automation, client-side secrets, log leakage, and restore integrity.
