# Backup / Restore

Export JSON for full fidelity and CSV for tabular records, with asset storage references.

Restore into a new environment, apply the same migrations, import data, verify record counts, verify asset references, open the application and reconcile missing objects.

A backup is not validated until restore succeeds.
