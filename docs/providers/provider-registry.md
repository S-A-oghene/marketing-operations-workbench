# Provider Registry

OpenRouter: optional; server-side key; dynamic free model collection; internal safety budget 45 requests/day.

Gemini: optional; server-side key; current provider policy/rates are time-sensitive.

Hugging Face: optional specialist lane; Spaces are replaceable; `SPACE_AVAILABLE`, `SPACE_UNAVAILABLE`, `SPACE_CHANGED`.

Kev: optional evaluator; no text generation; validate locally on Workbench tasks.

Manual AI: user-controlled browser fallback.
