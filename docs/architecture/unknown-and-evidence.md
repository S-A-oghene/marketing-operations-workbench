# Evidence and UNKNOWN semantics

A platform request being accepted is not publication evidence.

A metric without a source is `UNKNOWN`.

An application without actual confirmation is `READY_TO_SUBMIT` or `UNKNOWN`, not `SUBMITTED`.

Generated narrative is separated from measurement-store facts.
