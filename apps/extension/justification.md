# Extension permission justification

- `sidePanel`: required for the persistent side panel.
- `activeTab`: temporary access after an explicit user action.
- `scripting`: required to run a user-triggered capture function that reads selected text, page title and URL.

The extension does not capture cookies, authentication headers, passwords, unrelated browsing data, or full page contents by default.
